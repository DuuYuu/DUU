<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!--namespace=绑定一个指定的Dao/Mapper接口-->
<mapper namespace="com.atduu.mapper.BlogMapper">

    <select id="queryBlogById" resultType="Blog">
        select * from t_blog where id = #{id}
    </select>

    <select id="queryAllBlogs" resultType="Blog">
        select * from t_blog
    </select>

    <!--动态查询 + 模糊查询-->
    <select id="queryBlogByTitleOrTypeIdOrRecommend" parameterType="Blog" resultType="Blog">
        select b.id, b.title , b.update_time , b.recommend , b.type_id , t.id , t.name from t_blog b ,t_type t
        <where>
            <if test="1 == 1">
                b.type_id = t.id
            </if>
            <if test="title !=null and title !=''">
                and title like concat('%',#{title},'%')
            </if>
            <if test="typeId != null  and typeId !=''">
                and  b.type_id = #{typeId}
            </if>
            <if test="recommend !=null and recommend !=''">
                and recommend = #{recommend}
            </if>
        </where>
    </select>

    <insert id="save" parameterType="Blog">
        insert into t_blog values (#{id}, #{appreciation}, #{comment_able}, #{content}, #{create_time},
                                   #{first_picture}, #{flag}, #{published},#{recommend},
                                   #{share_statement}, #{title}, #{update_time}, #{views},
                                   #{type_id}, #{user_id})
    </insert>

    <update id="update" parameterType="Blog">
        update t_blog set id = #{id}, appreciation = #{appreciation}, comment_able = #{comment_able},
                          content = #{content}, create_time = #{create_time},first_picture = #{first_picture},
                          flag = #{flag},  published= #{published},recommend = #{recommend},
                          share_statement = #{share_statement}, title = #{title}, update_time =  #{update_time},
                          views = #{views}, type_id = #{type_id}, user_id = #{user_id}
    </update>

    <delete id="delete">
        delete from t_blog where id = #{id}
    </delete>

</mapper>